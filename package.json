{
  "name": "puppeteer-concurrent-worker-pool",
  "version": "1.0.0",
  "description": "A robust concurrent worker pool using Puppeteer with proxy rotation, unique user agents, and crash resilience.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "puppeteer": "^22.12.0",
    "user-agents": "^1.1.258"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}